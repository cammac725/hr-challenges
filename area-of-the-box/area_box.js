function numberOfWays(cases) {
    // need an output array, [0] * len(cases)
    // counter 1 for test case 1
    // counter 2 for test case 2
    // while a <= the lesser of N or M, min(N, M)
    // for a = 1, 1 x 1 bar would be N * M
    // (counter1=N*M)
    // a += 1


}
